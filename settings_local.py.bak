SITE_SRC_ROOT = '/home/qwertykitchen2/brntgarlic_code/CNPROG'

#for logging
import logging
LOG_FILENAME = './my_log.txt'
logging.basicConfig(filename=LOG_FILENAME,level=logging.DEBUG,)


DATABASE_NAME = 'brntgarlic_django'             # Or path to database file if using sqlite3.
DATABASE_USER = 'varius'               # Not used with sqlite3.
DATABASE_PASSWORD = 'e98bd7fr'               # Not used with sqlite3.
DATABASE_HOST = 'mysql.tastestalkr.com'               # Not used with sqlite3.


# Absolute path to the directory that holds media.
# Example: "/home/media/media.lawrence.com/"
MEDIA_ROOT = '/home/qwertykitchen2/'

# URL that handles the media served from MEDIA_ROOT. Make sure to use a
# trailing slash if there is a path component (optional in other cases).
# Examples: "http://media.lawrence.com", "http://example.com/media/"
MEDIA_URL = '/media/'


LANGUAGE_CODE = "en-US"